{% extends 'basedashboard.html.twig' %}
{% block title %}
    Medecin Dashboard  
{% endblock %}
{% block content %}
    <div class="col-md-7 col-lg-8 col-xl-9">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title mb-0">
                    <font style="vertical-align: inherit;">
                        <font style="vertical-align: inherit;">
                            Ajouter une consultation
                        </font>
                    </font>
                </h4>
            </div>
            <div class="card-body">

                <div class="card card-table">

                
            </a>
            <div class="card-body">

<div class="card card-table">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover table-center">
                <tbody>
                    <a href="https://meet.google.com/new" class="btn btn-lg btn-primary" target="_blank">Récupérer le lien de la consultation</a>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button type="submit" class="btn btn-sm btn-primary">Suivant</button>
                    {{ form_end(form) }}
                </tbody>
            </table>
        </div>
    </div>
</div>

                {% endblock %}