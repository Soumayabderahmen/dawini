<form method="post" action="{{ path('app_replay_sujet_delete', {'id': replay.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ replay.id) }}">
    <button class="tag-link"style="background-color: firebrick; color:black">Delete</button>
</form>
